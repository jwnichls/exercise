<div class="row survey" id="survey">
    <fieldset>
      <legend>Survey Required to Participate</legend>
        <%= form_for @survey, :remote => true, 'data-type'=>'json' do |f| %>

		<%= f.hidden_field :campaign_id %>
		<%= f.hidden_field :user_id %>

        Please respond to the statements and questions below. You must complete all items to submit the survey and participate.
        <div class="row likert">      
          <div class="large 12 columns">
              <label><%= @campaign.survey_question_one %></label><span class="error-label"></span>
              <%= f.radio_button :ls1, '1'  %> 
              <span class="radio-label">Strongly Disagree</span>
              <%= f.radio_button :ls1, '2' %> 
              <span class="radio-label">Somewhat Disagree</span>
              <%= f.radio_button :ls1, '3' %> 
              <span class="radio-label">Neither Agree nor Disagree</span>
              <%= f.radio_button :ls1, '4' %> 
              <span class="radio-label">Somewhat Agree</span>
              <%= f.radio_button :ls1, '5' %> 
              <span class="radio-label">Strongly Agree</span>
          </div>
        </div>
        <div class="row likert">
          <div class="large 12 columns">
              <label><%= @campaign.survey_question_two %></label><span class="error-label"></span>
              <%= f.radio_button :ils1, '1' %> 
              <span class="radio-label">Strongly Disagree</span>
              <%= f.radio_button :ils1, '2' %> 
              <span class="radio-label">Somewhat Disagree</span>
              <%= f.radio_button :ils1, '3' %> 
              <span class="radio-label">Neither Agree nor Disagree</span>
              <%= f.radio_button :ils1, '4' %> 
              <span class="radio-label">Somewhat Agree</span>
              <%= f.radio_button :ils1, '5' %> 
              <span class="radio-label">Strongly Agree</span>
          </div>
        </div>
        <div class="row likert">
          <div class="large 12 columns">
              <label><%= @campaign.survey_question_three %></label><span class="error-label"></span>
              <%= f.radio_button :ls2, '1' %> 
              <span class="radio-label">Strongly Disagree</span>
              <%= f.radio_button :ls2, '2' %> 
              <span class="radio-label">Somewhat Disagree</span>
              <%= f.radio_button :ls2, '3' %> 
              <span class="radio-label">Neither Agree nor Disagree</span>
              <%= f.radio_button :ls2, '4' %> 
              <span class="radio-label">Somewhat Agree</span>
              <%= f.radio_button :ls2, '5' %> 
              <span class="radio-label">Strongly Agree</span>
          </div>
        </div>
        <%= f.hidden_field :user_id, { :value => session[:uid]} %>
        <%= f.submit "Submit Survey Responses", class: "btn btn-large btn-primary", :id => 'surveyform' %>
      <% end %> 
  
    </fieldset>

    </div>
<%= form_for(@campaign) do |f| %>
  <% if @campaign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaign.errors.count, "error") %> prohibited this campaign from being saved:</h2>

      <ul>
      <% @campaign.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.collection_select(:user_id, User.all, :id, :t_screen_name, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :instructions %><br>
    <%= f.text_area :instructions %>
  </div>
  <div class="field">
    <%= f.label :survey_question_one %><br>
    <%= f.text_field :survey_question_one %>
  </div>
  <div class="field">
    <%= f.label :survey_question_two %><br>
    <%= f.text_field :survey_question_two %>
  </div>
  <div class="field">
    <%= f.label :survey_question_three %><br>
    <%= f.text_field :survey_question_three %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
